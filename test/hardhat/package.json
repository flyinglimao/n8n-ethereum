{
  "name": "n8n-ethereum-test-hardhat",
  "version": "1.0.0",
  "description": "Hardhat test environment for n8n-ethereum",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network localhost",
    "fund": "hardhat run scripts/fund-account.js --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0"
  }
}
